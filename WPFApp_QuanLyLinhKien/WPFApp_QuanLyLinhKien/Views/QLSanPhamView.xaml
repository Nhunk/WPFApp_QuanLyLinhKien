<Page x:Class="WPFApp_QuanLyLinhKien.Views.QLSanPhamView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPFApp_QuanLyLinhKien.Views"
      xmlns:ViewModels="clr-namespace:WPFApp_QuanLyLinhKien.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="QLSanPhamView">

    <StackPanel>

        <StackPanel Orientation="Horizontal" Margin="10">
            <Button Content="Thêm sản phẩm"
                Visibility="{Binding CanAdd, Converter={StaticResource BoolToVisibility}}"/>

            <Button Content="Xóa"
                Margin="10,0"
                Visibility="{Binding CanDelete, Converter={StaticResource BoolToVisibility}}"/>

            <Button Content="Sửa"
                Visibility="{Binding CanEdit, Converter={StaticResource BoolToVisibility}}"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Products}"
          AutoGenerateColumns="False"
          Margin="10"
          RowHeight="80">

            <DataGrid.Columns>

                <DataGridTextColumn Header="ID" Binding="{Binding SanPhamID}" Width="80"/>

                <DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding TenSP}" Width="200"/>

                <DataGridTextColumn Header="Loại SP" Binding="{Binding LoaiSanPhamID}" Width="100"/>

                <DataGridTextColumn Header="Giá" Binding="{Binding DonGia}" Width="100"/>

                <DataGridTextColumn Header="Mô tả" Binding="{Binding MoTa}" Width="250"/>

                <!-- Hình ảnh -->
                <DataGridTemplateColumn Header="Hình ảnh" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding HinhAnh, Converter={StaticResource ImgPath}}"
                           Width="100" Height="70"
                           Stretch="UniformToFill"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>

        </DataGrid>

    </StackPanel>
</Page>
