<Window x:Class="WPFApp_QuanLyLinhKien.Views.Staff.StaffDashboardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFApp_QuanLyLinhKien.Views.Staff"
        mc:Ignorable="d"
        Title="StaffDashboard" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1A237E">

            <DockPanel Margin="10">

                <TextBlock DockPanel.Dock="Left"
                           Foreground="White"
                           FontSize="20"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           Margin="0,0,20,0">
                    Staff Panel
                </TextBlock>

                <Button Content="Đăng xuất" 
                        DockPanel.Dock="Right" 
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Background="#38439E" 
                        Foreground="White"
                        Padding="10, 5"/>

                <ListBox x:Name="MainMenuListBox" 
                         Background="Transparent" 
                         BorderThickness="0"
                         Foreground="White" 
                         SelectionMode="Single"
                         VerticalAlignment="Center">

                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>

                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="15, 5"/>
                            <Setter Property="Margin" Value="2, 0"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#009688"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#38439E"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>

                    <ListBoxItem Content="Dashboard" IsSelected="True"/>
                    <ListBoxItem Content="Sản phẩm"/>
                    <ListBoxItem Content="Đơn hàng"/>
                    <ListBoxItem Content="Khách hàng"/>
                </ListBox>

            </DockPanel>
        </Border>

        <Frame Grid.Row="1" x:Name="StaffFrame" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
